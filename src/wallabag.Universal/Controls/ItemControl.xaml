<UserControl
    x:Class="wallabag.Controls.ItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:wallabag.Controls"
    mc:Ignorable="d"
    d:DesignWidth="350"
    d:DesignHeight="300">

    <UserControl.Resources>

        <Storyboard x:Name="ShowOverlayStoryboard">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="OverlayGrid">
                <DiscreteObjectKeyFrame KeyTime="0">
                    <DiscreteObjectKeyFrame.Value>
                        <Visibility>Visible</Visibility>
                    </DiscreteObjectKeyFrame.Value>
                </DiscreteObjectKeyFrame>
            </ObjectAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="OverlayGrid">
                <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
        <Storyboard x:Name="HideOverlayStoryboard">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="OverlayGrid">
                <DiscreteObjectKeyFrame KeyTime="0:0:0.3">
                    <DiscreteObjectKeyFrame.Value>
                        <Visibility>Collapsed</Visibility>
                    </DiscreteObjectKeyFrame.Value>
                </DiscreteObjectKeyFrame>
            </ObjectAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="OverlayGrid">
                <SplineDoubleKeyFrame KeyTime="0" Value="1"/>
                <SplineDoubleKeyFrame KeyTime="0:0:0.3" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

    </UserControl.Resources>

    <Grid x:Name="RootGrid" Height="250">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.Setters>
                        <Setter Target="OverlayGrid.Visibility" Value="Visible"/>
                        <Setter Target="OverlayGrid.Opacity" Value="1"/>
                        <Setter Target="RootGrid.Height" Value="200"/>
                        <Setter Target="TagsTextBlock.Visibility" Value="Collapsed"/>
                        <Setter Target="PropertyIndicatorStackPanel.Visibility" Value="Collapsed"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Width" Value="350"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720"/>
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill" ImageSource="{Binding Model.PreviewPictureUri, Mode=OneTime}"/>
        </Grid.Background>
        <Grid x:Name="BottomGrid" Grid.Row="1"
              RequestedTheme="Dark">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#33000000" Offset="0"/>
                    <GradientStop Color="#33000000" Offset="1"/>
                    <GradientStop Color="#66000000" Offset="0.5"/>
                </LinearGradientBrush>
            </Grid.Background>
            <StackPanel VerticalAlignment="Center">
                <TextBlock 
                    Text="{Binding Model.Title, Mode=OneTime}" 
                    Margin="8"
                    FontSize="16"
                    TextAlignment="Center"
                    MaxLines="2" TextWrapping="Wrap"/>
                <TextBlock
                    Text="{Binding Model.DomainName, Mode=OneTime}"
                    TextAlignment="Center"
                    Opacity="0.8" FontSize="13.333"/>
            </StackPanel>
        </Grid>
        <Grid x:Name="OverlayGrid" Grid.RowSpan="2" Visibility="Collapsed" Opacity="0" RequestedTheme="Dark">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#4C000000" Offset="0"/>
                    <GradientStop Color="#4C000000" Offset="1"/>
                    <GradientStop Offset="0.5" Color="#00000000"/>
                </LinearGradientBrush>
            </Grid.Background>
            <TextBlock x:Name="TagsTextBlock"
                Text="{Binding TagsString, Mode=OneWay}"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="12,12,64,12"
                Opacity="0.8" FontSize="13.333"/>
            <StackPanel x:Name="PropertyIndicatorStackPanel"
                Orientation="Horizontal"
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Opacity="0.8"
                Margin="12">
                <TextBlock
                    Visibility="{Binding Model.IsStarred, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Text="&#xE00A;"
                    FontFamily="Segoe MDL2 Assets"/>
                <TextBlock
                    Visibility="{Binding Model.IsRead, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Margin="8,0,0,0"
                    Text="&#xE001;" 
                    FontFamily="Segoe MDL2 Assets"/>
            </StackPanel>
            <TextBlock
                Text="{Binding PublishedDateFormatted, Mode=OneTime}"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="12"
                Opacity="0.8" FontSize="13.333"/>
            <StackPanel
                Orientation="Horizontal"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="12">
                <TextBlock
                    Text="&#xE121;"
                    FontFamily="Segoe MDL2 Assets"
                    Opacity="0.8"
                    VerticalAlignment="Center"/>
                <TextBlock
                    Text="{Binding Model.EstimatedReadingTime, Mode=OneTime}"
                    Opacity="0.8"
                    Margin="4,0"
                    FontSize="13.333"
                    VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
