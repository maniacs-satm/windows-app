<UserControl
    x:Class="wallabag.Controls.ItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:wallabag.Controls"
    mc:Ignorable="d"
    d:DesignWidth="350"
    d:DesignHeight="300">

    <Grid x:Name="RootGrid" Width="350" Height="250">
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill" ImageSource="{Binding Model.PreviewPictureUri, Mode=OneTime}"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid x:Name="BottomGrid" Grid.Row="1"
              RequestedTheme="Dark">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#00000000" Offset="0"/>
                    <GradientStop Color="#7F000000" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <TextBlock Text="{Binding Model.Title, Mode=OneTime}" 
                       Margin="16"
                       FontSize="16"
                       VerticalAlignment="Bottom"
                       MaxLines="2" TextWrapping="Wrap"/>
        </Grid>
        <Grid x:Name="OverlayGrid" Grid.RowSpan="2" Visibility="Collapsed"
              RequestedTheme="Dark" Background="#CC000000">
            <StackPanel VerticalAlignment="Center">
                <TextBlock 
                    Text="{Binding Model.Title, Mode=OneTime}" 
                    Margin="8"
                    FontSize="16"
                    TextAlignment="Center"
                    MaxLines="2" TextWrapping="Wrap"/>
                <TextBlock
                    Text="{Binding Model.DomainName, Mode=OneTime}"
                    FontWeight="Bold"
                    TextAlignment="Center"
                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
            </StackPanel>
            <TextBlock
                Text="{Binding PublishedDateFormatted, Mode=OneTime}"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="12"
                Opacity="0.8" FontSize="13.333"/>
            <StackPanel
                Orientation="Horizontal"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="12">
                <TextBlock
                    Text="&#xE121;"
                    FontFamily="Segoe MDL2 Assets"
                    Opacity="0.8"
                    FontSize="13.333"
                    VerticalAlignment="Center"/>
                <TextBlock
                    Text="{Binding Model.EstimatedReadingTime, Mode=OneTime}"
                    Opacity="0.8"
                    Margin="4,0"
                    FontSize="13.333"
                    VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <Grid x:Name="ContextMenuGrid" Grid.RowSpan="2" Background="Transparent">
            <Grid.Resources>
                <Storyboard x:Name="ShowContextMenu">
                    <ColorAnimation Duration="0:0:0.2" To="#CCFFFFFF" Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)" Storyboard.TargetName="ContextMenuGrid" d:IsOptimized="True"/>
                    <DoubleAnimation Duration="0:0:0.2" To="1" Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="stackPanel" d:IsOptimized="True"/>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="stackPanel">
                        <DiscreteObjectKeyFrame KeyTime="0">
                            <DiscreteObjectKeyFrame.Value>
                                <Visibility>Visible</Visibility>
                            </DiscreteObjectKeyFrame.Value>
                        </DiscreteObjectKeyFrame>
                    </ObjectAnimationUsingKeyFrames>
                </Storyboard>
                <Storyboard x:Name="HideContextMenu">
                    <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)" Storyboard.TargetName="ContextMenuGrid">
                        <SplineColorKeyFrame KeyTime="0:0:0.2" Value="Transparent"/>
                    </ColorAnimationUsingKeyFrames>
                    <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="stackPanel">
                        <SplineDoubleKeyFrame KeyTime="0:0:0.2" Value="0"/>
                    </DoubleAnimationUsingKeyFrames>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="stackPanel">
                        <DiscreteObjectKeyFrame KeyTime="0:0:0.2">
                            <DiscreteObjectKeyFrame.Value>
                                <Visibility>Collapsed</Visibility>
                            </DiscreteObjectKeyFrame.Value>
                        </DiscreteObjectKeyFrame>
                    </ObjectAnimationUsingKeyFrames>
                </Storyboard>
            </Grid.Resources>
            <StackPanel x:Name="stackPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal" Visibility="Collapsed" Opacity="0">
                <AppBarButton IsCompact="True" Icon="{Binding Model.IsRead, Converter={StaticResource ReadToIconConverter}}" Command="{Binding SwitchReadStatusCommand}"/>
                <AppBarButton IsCompact="True" Icon="{Binding Model.IsStarred, Converter={StaticResource FavoriteToIconConverter}}" Command="{Binding SwitchFavoriteStatusCommand}"/>
                <AppBarButton IsCompact="True" Command="{Binding ShareCommand}">
                    <AppBarButton.Icon>
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72D;"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton IsCompact="True" Icon="World" Command="{Binding OpenInBrowserCommand}"/>
                <AppBarButton IsCompact="True" Icon="Delete" Command="{Binding DeleteCommand}"/>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
