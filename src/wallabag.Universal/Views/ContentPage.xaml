<Page
    x:Class="wallabag.Views.ContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:wallabag.Controls"
    xmlns:vm="using:wallabag.ViewModels"
    mc:Ignorable="d">
    <Page.DataContext>
        <vm:MainViewModel/>
    </Page.DataContext>

    <Page.BottomAppBar>
        <CommandBar x:Name="PrimaryCommandBar">
            <AppBarToggleButton x:Name="filterToggleButton" Icon="Filter" Label="Filter"/>
            <AppBarButton Icon="Add" Label="Add item" Click="AddItemButton_Click"/>
            <AppBarButton Icon="Refresh" Label="Refresh" Command="{Binding RefreshCommand}"/>
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Setting" Label="Settings" Command="{Binding NavigateToSettingsPageCommand}"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.Setters>
                        <Setter Target="ContentDialogStackPanel.MinWidth" Value="0"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState>
                    <VisualState.Setters>
                        <Setter Target="ContentDialogStackPanel.MinWidth" Value="450"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720"/>
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <ContentDialog x:Name="AddItemContentDialog" Title="ADD LINK" d:IsHidden="True"
                       PrimaryButtonText="Add" PrimaryButtonCommand="{Binding AddItemCommand}"
                       SecondaryButtonText="Cancel" SecondaryButtonCommand="{Binding CancelCommand}">
            <ContentDialog.DataContext>
                <vm:AddItemPageViewModel/>
            </ContentDialog.DataContext>
            <StackPanel x:Name="ContentDialogStackPanel" MinWidth="450" Margin="0,24,0,0">
                <TextBox x:Name="UrlTextBox" Text="{Binding Url, Mode=TwoWay}" PlaceholderText="http://" InputScope="Url"/>
                <controls:TagControl Tags="{Binding Tags, Mode=TwoWay}" Margin="0,12,0,0" BorderThickness="2"/>
            </StackPanel>
        </ContentDialog>
        <SplitView PanePlacement="Right" IsPaneOpen="{Binding IsChecked, ElementName=filterToggleButton, Mode=TwoWay}">
            <GridView x:Name="ItemGridView"
        	ItemsSource="{Binding Items}"
        	IsItemClickEnabled="True"
            Padding="8"
            Margin="0,48,0,0"
        	ItemClick="ItemGridView_ItemClick"
        	EntranceNavigationTransitionInfo.IsTargetElement="True">
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="vm:ItemViewModel">
                        <controls:ItemControl/>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
            <SplitView.Pane>
                <StackPanel Margin="0,48" Padding="8">
                    <AutoSuggestBox x:Name="SearchBox"               
                                 QueryIcon="Find"
                                 PlaceholderText="Search..."
                                 VerticalAlignment="Top"
                                 HorizontalAlignment="Stretch"
                                 ItemsSource="{x:Bind SearchBoxSuggestions}"
                                 TextChanged="SearchBox_TextChanged" Margin="0,0,0,8"/>
                    <ComboBox Header="Domain name" Margin="0,0,0,8"/>
                    <DatePicker Header="Creation date" Margin="0,0,0,8"/>
                    <Slider Header="Estimated reading time (in minutes)" Margin="0,0,0,8"/>
                </StackPanel>
            </SplitView.Pane>
        </SplitView>
        <Grid Background="{ThemeResource SystemControlBackgroundAccentBrush}" Height="48" VerticalAlignment="Top" RequestedTheme="Dark">
            <TextBlock Text="ITEMS" Style="{ThemeResource PageTitleTextBlockStyle}" VerticalAlignment="Top"/>
        </Grid>
    </Grid>
</Page>
