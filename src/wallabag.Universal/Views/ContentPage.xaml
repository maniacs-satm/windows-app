<Page
    x:Class="wallabag.Views.ContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:wallabag.Controls"
    xmlns:vm="using:wallabag.ViewModels"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GridView x:Name="ItemGridView"
                  Grid.Row="1"
                  Padding="4"
                  Background="{ThemeResource SystemControlPageBackgroundListLowBrush}"
                  ItemsSource="{Binding Items}"
                  IsItemClickEnabled="True"
                  ItemClick="ItemGridView_ItemClick"
                  EntranceNavigationTransitionInfo.IsTargetElement="True">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="vm:ItemViewModel">
                    <Grid Width="300" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Image x:Name="Image" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Source="https://farm4.staticflickr.com/3955/15123553843_6b4a5c8a3f_z_d.jpg" Stretch="UniformToFill"/>
                        <TextBlock TextWrapping="Wrap" Text="{x:Bind Model.Title}" TextTrimming="CharacterEllipsis"
                                   MaxLines="2" TextAlignment="Left" VerticalAlignment="Bottom" Margin="10,15"
                                   Style="{ThemeResource SubtitleTextBlockStyle}" Grid.Row="1"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <Grid Background="{ThemeResource SystemControlBackgroundAccentBrush}" RequestedTheme="Dark">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <AutoSuggestBox x:Name="SearchBox" QueryIcon="Find" Width="300" PlaceholderText="Search..." Margin="4"
                                TextChanged="SearchBox_TextChanged" KeyDown="SearchBox_KeyDown" VerticalAlignment="Center"
                                ItemsSource="{x:Bind SearchBoxSuggestions}"/>
                <AppBarButton Icon="Find" Label="Search" IsCompact="True" Visibility="Collapsed"/>
            </StackPanel>            
            <TextBlock Text="ITEMS" Style="{ThemeResource PageTitleTextBlockStyle}" VerticalAlignment="Center" Margin="16,0"/>
        </Grid>
        <CommandBar VerticalAlignment="Bottom" HorizontalAlignment="Right" Grid.Row="1">
            <AppBarButton Icon="Add" Label="Add item" Click="AddItemButton_Click"/>
            <AppBarButton Icon="Refresh" Label="Refresh" Command="{Binding RefreshCommand}"/>
        </CommandBar>
        <ContentDialog x:Name="AddItemContentDialog" Title="ADD LINK" Grid.RowSpan="2"
                       PrimaryButtonText="Add" PrimaryButtonCommand="{Binding AddItemCommand}"
                       SecondaryButtonText="Cancel" SecondaryButtonCommand="{Binding CancelCommand}">
            <ContentDialog.DataContext>
                <vm:AddItemContentDialogViewModel/>
            </ContentDialog.DataContext>
            <StackPanel Width="450" Margin="0,24,0,0">
                <TextBox x:Name="UrlTextBox" Text="{Binding Url, Mode=TwoWay}" PlaceholderText="http://"/>
                <controls:TagControl Tags="{Binding Tags, Mode=TwoWay}" Margin="0,12,0,0"/>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>
