<Page
    x:Class="wallabag.Views.ContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:wallabag.Controls"
    xmlns:vm="using:wallabag.ViewModels"
    mc:Ignorable="d">
    <Page.DataContext>
        <vm:MainViewModel/>
    </Page.DataContext>

    <Page.BottomAppBar>
        <CommandBar x:Name="PrimaryCommandBar">
            <AppBarButton Icon="Filter" Label="Filter">
                <AppBarButton.Flyout>
                    <Flyout x:Name="filterFlyout" Placement="Full">
                        <StackPanel Margin="8">
                            <AutoSuggestBox x:Name="SearchBox"               
                                 QueryIcon="Find"
                                 PlaceholderText="Search..."
                                 VerticalAlignment="Top"
                                 HorizontalAlignment="Stretch"
                                 ItemsSource="{x:Bind SearchBoxSuggestions}"
                                 TextChanged="SearchBox_TextChanged" Margin="0,0,0,8"/>
                            <ComboBox Header="Domain name" Margin="0,0,0,8"/>
                            <DatePicker Header="Creation date" Margin="0,0,0,8" IsEnabled="False"/>
                            <Slider Header="Estimated reading time (in minutes)" Margin="0,0,0,8"/>
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <!--<AppBarButton x:Name="SearchButton" Icon="Find" Label="Search" Click="SearchButton_Click" HorizontalAlignment="Right" IsCompact="True"/>-->
            <AppBarButton Icon="Add" Label="Add item" Click="AddItemButton_Click"/>
            <AppBarButton Icon="Refresh" Label="Refresh" Command="{Binding RefreshCommand}"/>
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Setting" Label="Settings" Command="{Binding NavigateToSettingsPageCommand}"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.Setters>
                        <Setter Target="ContentDialogStackPanel.MinWidth" Value="0"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState>
                    <VisualState.Setters>
                        <Setter Target="ContentDialogStackPanel.MinWidth" Value="450"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720"/>
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Pivot>
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Style="{StaticResource PageTitleTextBlockStyle}" Margin="0"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <PivotItem Header="UNREAD">
                <GridView x:Name="ItemGridView"
                          ItemsSource="{Binding Items}"
                          IsItemClickEnabled="True"
                          ItemClick="ItemGridView_ItemClick"
                          EntranceNavigationTransitionInfo.IsTargetElement="True">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="vm:ItemViewModel">
                            <Grid Width="300" Height="400" Background="#0C000000">
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <Image x:Name="Image" Source="{Binding Model.HeaderImageUri}" Stretch="UniformToFill"/>
                                <Grid Grid.Row="1" Margin="8">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition />
                                        <RowDefinition Height="auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock
                                        TextWrapping="Wrap"
                                        TextTrimming="CharacterEllipsis"
                                        MaxLines="2"
                                        FontWeight="SemiBold"
                                        FontSize="20"
                                        Margin="0,8"
                                        Text="{x:Bind Model.Title}"
                                        Style="{ThemeResource TitleTextBlockStyle}"/>
                                    <TextBlock
                                        TextWrapping="Wrap"
                                        TextTrimming="WordEllipsis"
                                        VerticalAlignment="Top"
                                        MaxLines="3"
                                        Margin="0,8"
                                        LineHeight="1.3"
                                        FontWeight="Normal"
                                        Foreground="#666"
                                        Style="{ThemeResource BaseTextBlockStyle}"
                                        Grid.Row="1"
                                        Text="{x:Bind IntroSentence}"/>
                                    <StackPanel Orientation="Horizontal" Grid.Row="2">
                                        <SymbolIcon Symbol="Clock"/>
                                        <TextBlock Margin="8,0" VerticalAlignment="Bottom" Text="{x:Bind Model.EstimatedReadingTime}"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>
            <PivotItem Header="FAVORITES"/>
            <PivotItem Header="ARCHIVE"/>
            <PivotItem Header="TAGS"/>
        </Pivot>
        <ContentDialog x:Name="AddItemContentDialog" Title="ADD LINK" d:IsHidden="True"
                       PrimaryButtonText="Add" PrimaryButtonCommand="{Binding AddItemCommand}"
                       SecondaryButtonText="Cancel" SecondaryButtonCommand="{Binding CancelCommand}">
            <ContentDialog.DataContext>
                <vm:AddItemPageViewModel/>
            </ContentDialog.DataContext>
            <StackPanel x:Name="ContentDialogStackPanel" MinWidth="450" Margin="0,24,0,0">
                <TextBox x:Name="UrlTextBox" Text="{Binding Url, Mode=TwoWay}" PlaceholderText="http://" InputScope="Url"/>
                <controls:TagControl Tags="{Binding Tags, Mode=TwoWay}" Margin="0,12,0,0" BorderThickness="2"/>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>
