<Page
    x:Class="wallabag.Views.ContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:wallabag.Controls"
    xmlns:data="using:wallabag.DataModel"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          Loaded="Grid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <CommandBar x:Name="MainCommandBar" VerticalAlignment="Top" HorizontalContentAlignment="Stretch">
            <AppBarButton Icon="Add" Label="Add"/>
            <AppBarButton Icon="Refresh" Label="Refresh" Command="{Binding RefreshCommand}"/>
            <CommandBar.Content>
                <controls:PageHeader>
                    <controls:PageHeader.HeaderContent>
                        <TextBlock Text="ITEMS" Style="{ThemeResource PageTitleTextBlockStyle}" TextWrapping="NoWrap"/>
                    </controls:PageHeader.HeaderContent>
                </controls:PageHeader>
            </CommandBar.Content>
        </CommandBar>
        <GridView x:Name="ItemListView" ItemsSource="{Binding Items}" Grid.Row="1"
                          IsItemClickEnabled="True" ItemClick="ItemListView_ItemClick" EntranceNavigationTransitionInfo.IsTargetElement="True">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:ItemViewModel">
                    <RelativePanel Width="300" Background="#333">
                        <Image x:Name="Image" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                         RelativePanel.AlignHorizontalCenterWithPanel="True"
                         RelativePanel.AlignVerticalCenterWithPanel="True" Source="https://jlnostr.de/content/images/2015/02/books.jpg" Stretch="UniformToFill"/>
                        <Border RelativePanel.AlignVerticalCenterWith="Image" RelativePanel.AlignHorizontalCenterWith="Image" Margin="10">
                            <StackPanel RequestedTheme="Dark" Margin="50">
                                <TextBlock TextWrapping="Wrap" Text="{x:Bind Model.Title}" TextTrimming="CharacterEllipsis" MaxLines="2" TextAlignment="Center"
                                 Style="{ThemeResource SubtitleTextBlockStyle}"/>

                                <TextBlock TextWrapping="Wrap" Text="{x:Bind UrlHostname}" Style="{StaticResource BaseTextBlockStyle}"
                           Foreground="{ThemeResource ItemHostnameBrush}" Margin="0,5" TextAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </RelativePanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <ProgressRing Height="100" Width="100" IsActive="True" Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.Row="1"/>
    </Grid>
</Page>
