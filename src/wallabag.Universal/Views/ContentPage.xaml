<Page
    x:Class="wallabag.Views.ContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:wallabag.Controls"
    xmlns:vm="using:wallabag.ViewModels"
    mc:Ignorable="d">
    <Page.Resources>
        <Storyboard x:Name="ShowSearchBox">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="SearchBox">
                <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="SearchBox">
                <DiscreteObjectKeyFrame KeyTime="0">
                    <DiscreteObjectKeyFrame.Value>
                        <Visibility>Visible</Visibility>
                    </DiscreteObjectKeyFrame.Value>
                </DiscreteObjectKeyFrame>
            </ObjectAnimationUsingKeyFrames>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="SearchButton">
                <DiscreteObjectKeyFrame KeyTime="0">
                    <DiscreteObjectKeyFrame.Value>
                        <Visibility>Collapsed</Visibility>
                    </DiscreteObjectKeyFrame.Value>
                </DiscreteObjectKeyFrame>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>
        <Storyboard x:Name="HideSearchBox">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="SearchBox">
                <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:1" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="SearchBox">
                <DiscreteObjectKeyFrame KeyTime="0:0:1">
                    <DiscreteObjectKeyFrame.Value>
                        <Visibility>Collapsed</Visibility>
                    </DiscreteObjectKeyFrame.Value>
                </DiscreteObjectKeyFrame>
            </ObjectAnimationUsingKeyFrames>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="SearchButton">
                <DiscreteObjectKeyFrame KeyTime="0:0:1">
                    <DiscreteObjectKeyFrame.Value>
                        <Visibility>Visible</Visibility>
                    </DiscreteObjectKeyFrame.Value>
                </DiscreteObjectKeyFrame>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.Setters>
                        <Setter Target="ItemGridView.Visibility" Value="Collapsed"/>
                        <Setter Target="ItemListView.Visibility" Value="Visible"/>
                        <Setter Target="ContentDialogStackPanel.MinWidth" Value="0"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState>
                    <VisualState.Setters>
                        <Setter Target="ItemGridView.Visibility" Value="Visible"/>
                        <Setter Target="ItemListView.Visibility" Value="Collapsed"/>
                        <Setter Target="ContentDialogStackPanel.MinWidth" Value="450"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720"/>
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <GridView x:Name="ItemGridView"
                  Margin="0,48,0,0"
                  Padding="4"
                  Background="{ThemeResource SystemControlPageBackgroundListLowBrush}"
                  ItemsSource="{Binding Items}"
                  IsItemClickEnabled="True"
                  ItemClick="ItemGridView_ItemClick"
                  EntranceNavigationTransitionInfo.IsTargetElement="True">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="vm:ItemViewModel">
                    <Grid Width="300" Height="300" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <Image x:Name="Image" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Source="https://jlnostr.de/content/images/2015/06/header_mesonpress-2-3.jpg" Stretch="UniformToFill"/>
                        <TextBlock TextWrapping="Wrap" Text="{x:Bind Model.Title}" TextTrimming="CharacterEllipsis"
                                   MaxLines="2" TextAlignment="Left" VerticalAlignment="Bottom" Margin="10,15"
                                   Style="{ThemeResource SubtitleTextBlockStyle}" Grid.Row="1"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <ListView x:Name="ItemListView"
                  ItemsSource="{Binding Items}"
                  IsItemClickEnabled="True"
                  ItemClick="ItemGridView_ItemClick"
                  EntranceNavigationTransitionInfo.IsTargetElement="True"
                  Margin="0,48,0,0"
                  Padding="0,8">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:ItemViewModel">
                    <Grid Height="64">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="64"/>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Image x:Name="Image" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Source="https://farm4.staticflickr.com/3955/15123553843_6b4a5c8a3f_z_d.jpg" Stretch="UniformToFill"/>
                        <TextBlock Text="{x:Bind Model.Title}"
                                   TextTrimming="CharacterEllipsis"
                                   MaxLines="1" VerticalAlignment="Center" Margin="12,0"
                                   Style="{ThemeResource SubtitleTextBlockStyle}" Grid.Column="1"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <CommandBar x:Name="PrimaryCommandBar" VerticalAlignment="Top">
            <AppBarButton x:Name="SearchButton" Icon="Find" Label="Search" Click="SearchButton_Click"/>
            <AppBarSeparator/>
            <AppBarButton Icon="Add" Label="Add item" Click="AddItemButton_Click"/>
            <AppBarButton Icon="Refresh" Label="Refresh" Command="{Binding RefreshCommand}"/>
            <CommandBar.Content>
                <TextBlock Text="ITEMS" Style="{ThemeResource PageTitleTextBlockStyle}"/>
            </CommandBar.Content>
        </CommandBar>
        <AutoSuggestBox x:Name="SearchBox"
                QueryIcon="Find"
                Width="300"
                PlaceholderText="Search..."
                VerticalAlignment="Top"
                Margin="8,8,220,8"
                HorizontalAlignment="Right"
                ItemsSource="{x:Bind SearchBoxSuggestions}"
                TextChanged="SearchBox_TextChanged"
                KeyDown="SearchBox_KeyDown"
                LostFocus="SearchBox_LostFocus"
                Opacity="0"
                Visibility="Collapsed"/>
        <ContentDialog x:Name="AddItemContentDialog" Title="ADD LINK" d:IsHidden="True"
                       PrimaryButtonText="Add" PrimaryButtonCommand="{Binding AddItemCommand}"
                       SecondaryButtonText="Cancel" SecondaryButtonCommand="{Binding CancelCommand}">
            <ContentDialog.DataContext>
                <vm:AddItemContentDialogViewModel/>
            </ContentDialog.DataContext>
            <StackPanel x:Name="ContentDialogStackPanel" MinWidth="450" Margin="0,24,0,0">
                <TextBox x:Name="UrlTextBox" Text="{Binding Url, Mode=TwoWay}" PlaceholderText="http://" InputScope="Url"/>
                <controls:TagControl Tags="{Binding Tags, Mode=TwoWay}" Margin="0,12,0,0" BorderThickness="2"/>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>
