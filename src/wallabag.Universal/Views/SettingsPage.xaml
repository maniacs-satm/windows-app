<Page
    x:Class="wallabag.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:wallabag.Controls"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <controls:PageHeader>
            <controls:PageHeader.HeaderContent>
                <CommandBar VerticalAlignment="Top" HorizontalContentAlignment="Stretch" Background="{ThemeResource SystemControlBackgroundAccentBrush}" RequestedTheme="Dark">
                    <AppBarButton IsEnabled="False" Icon="Undo" Label="Reset"/>                   
                    <CommandBar.Content>
                        <controls:PageHeader>
                            <controls:PageHeader.HeaderContent>
                                <TextBlock Text="SETTINGS" Style="{ThemeResource PageTitleTextBlockStyle}"/>
                            </controls:PageHeader.HeaderContent>
                        </controls:PageHeader>
                    </CommandBar.Content>
                </CommandBar>
            </controls:PageHeader.HeaderContent>
        </controls:PageHeader>
        <Pivot Grid.Row="1">
            <Pivot.Resources>
                <Style TargetType="ToggleSwitch">
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0,8"/>
                </Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Margin" Value="0,12,0,0"/>
                </Style>
                <Style TargetType="TextBox">
                    <Setter Property="Margin" Value="0,10"/>
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="Width" Value="350"/>
                </Style>
            </Pivot.Resources>
            <PivotItem Header="General">
                <StackPanel>
                    <TextBlock Text="To apply the following settings you need to restart the app." TextWrapping="Wrap" Margin="0,0,0,12" FontStyle="Italic"/>
                    <ComboBox Header="Language" IsEnabled="False" Width="320" Margin="0,8"/>
                    <ToggleSwitch x:Name="useSystemAccentColorToggleSwitch" Header="Use system accent color" IsOn="{Binding AppSettings.UseSystemAccentColor, Mode=TwoWay}"/>
                    <ToggleSwitch Header="Position of menu button" OnContent="Right" OffContent="Left" IsOn="{Binding AppSettings.HamburgerPositionIsRight, Mode=TwoWay}"/>
                </StackPanel>
            </PivotItem>
            <PivotItem Header="Account credentials">
                <StackPanel>
                    <TextBox x:Name="UsernameTextBox" Header="Username" Text="{Binding AppSettings.Username, Mode=TwoWay}"/>
                    <PasswordBox x:Name="PasswordTextBox" Header="Password" Password="{Binding AppSettings.Password, Mode=TwoWay}" Width="350" HorizontalAlignment="Left"/>
                    <TextBox x:Name="UrlTextBox" Header="Url to wallabag" InputScope="Url" PlaceholderText="http://" Text="{Binding AppSettings.wallabagUrl, Mode=TwoWay}"/>
                </StackPanel>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
